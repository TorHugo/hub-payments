############
## CONFIG ##
############
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hub_payment
    username: postgres
    password: 10021977
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  messages:
    basename: messages

############
## TOKEN  ##
############
access:
  token:
    value: ${access_token}

############
## CLIENT ##
############
request:
  host:
    value: https://sandbox.asaas.com/api/v3
  endpoint:
    client:
      creat_client:
        value: /customers
    credit_card:
      tokenize:
        value: /creditCard/tokenize
    payment:
      value: /payments
    refund:
      value: /refund